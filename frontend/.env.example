NEXT_PUBLIC_API_URL=http://localhost:8000
# Legacy alias — kept for older HealthCheck components. `NEXT_PUBLIC_API_URL`
# takes precedence when both are set.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
